# The box ledger

**Rules version:** 2026-09-24.3 (sections 1 to 11)
**Copies:** `docs/box-ledger.md` in `TNMD-FDO/GIDEON-dev` (GIDEON's development repository, private) and `docs/box-ledger.md` in `TNMD-FDO/gideon-transcribe`. The public `TNMD-FDO/GIDEON` carries a copy in GIDEON's export at each tag, lagging between tags; it is never the copy fetched. Sections 1 to 11 are one text in both; section 12 is a log with one subsection per project, each written only by that project. Nobody writes into the other project's repository: each project fetches the other's copy and merges it (§11).

Two projects run on one server and in one GitHub organisation: GIDEON, the platform and legal-AI product, and Gideon Transcribe, the transcription app. Each has its own repository, install home, release rule, and operator. GIDEON is the authoritative service on the server, the one it is provisioned and sized for; Gideon Transcribe is a secondary service, for transcription, that fits into the room GIDEON's allocation leaves. They share the hardware, the Docker daemon, the host firewall, the nightly hours, and the organisation's pool of hosted-runner minutes. This ledger records who owns each shared thing, what each project may do with it, the events one project must announce to the other, and, in its log, what each project changed and what it recommends to the other. It states keys, defaults, paths, and units. It never states an office value: no hostname, address, network range, account name, or storage target appears here, because both repositories are written as if public.

The facts below were read from the two repositories and from the running server on the ledger's date. A number marked *observed* is what the server showed that day, not a commitment.

## 1. The two projects

| | GIDEON | Gideon Transcribe |
|---|---|---|
| Repository | `TNMD-FDO/GIDEON-dev` (development, private); the public `TNMD-FDO/GIDEON` receives a filtered export of each tag | `TNMD-FDO/gideon-transcribe` |
| What it is | the platform (host provisioning, rendering, apply, backups, observability) and the legal-AI branches | a Django app, a pinned WhisperX service, and workers for transcription, translation, and diarisation |
| Standing on the server | the authoritative service: the host is provisioned, sized, and scheduled for it, and its allocation comes first | a secondary service for transcription, fitted into what GIDEON's allocation leaves free |
| Install home | `/opt/gideon`, a clone of the development checkout owned by the dev seat and upgraded at a tag | `/opt/gideon-transcribe` |
| Operator command | `python3 -m gideon …` | `./transcribe …` |
| Compose project | `gideon`, and the standing developer sibling `gideon-ci` (plus `gideon-drill` during a drill and `gideon-ci-contract` and `gideon-ci-sentinel` during CI) | `transcribe` (plus a drill project under its own directory) |
| Release rule | code reaches the server only at a tag, through `upgrade` | the same rule, suspended for this one server until its `v1.0.0` (its ADR 0007) |
| Where its agents learn of this ledger | `CLAUDE.md` (the shared server paragraph), the architecture map §17, the plan template's Documentation Impact, the release skill's Step 10 | `CLAUDE.md` (the shared server section) and its ADR 0003, which names the shared engine as the one coupling |

**Precedence.** GIDEON is the authoritative service on this server and Gideon Transcribe a secondary one. Where the two need the same thing, a card's memory, a night's hour, the hosted-minute pool, a port, a directory, the disk, GIDEON's need is met first and Transcribe fits into what remains. GIDEON's specification, decision records, and locks are the authority for the host, and this ledger's tables are read in that light: a reserve GIDEON's plan writes for Transcribe is what Transcribe may count on, and anything beyond it is on loan until GIDEON needs it. Precedence is not licence to break Transcribe without notice: the announce-before list (§8) stands, taking back a loan is a rules change here first with the notice a maintenance window gets, and a secondary service that is running is left running until its stop or its replacement is agreed.

**Direction and request.** The same precedence runs through the log (§12). A line GIDEON addresses to Transcribe about a shared thing, or about a design change GIDEON has made, is a direction: Transcribe's agents fit their next change to it and answer by number with what changed and when, or, when it cannot be followed, with the reason, within the notice a maintenance window gets, so the two operators can settle it. A line Transcribe addresses to GIDEON is a request that GIDEON weighs and answers. Transcribe's own product decisions stay its own where they touch nothing shared.

## 2. Who owns the host

GIDEON's `host provision` owns the operating-system layer. Its steps, by name, and what each owns:

| Step | Owns |
|---|---|
| `firewall` | ufw and its allow-list from the site file's LAN networks, and the `DOCKER-USER` block that makes that list true for Docker-published ports (§4) |
| `nvidia-driver`, `nvidia-toolkit` | the driver branch and version from `host.lock`, the container toolkit, CDI device naming |
| `docker-engine` | Docker Engine and Compose at the lock's minimums, `/etc/docker/daemon.json` (data root, journald logging), `/etc/containerd/config.toml` (containerd's root on the Docker volume, the image store's home) |
| `disk-layout` | the `/data` volume and its managed directories (§6) |
| `unattended-upgrades` | Ubuntu's security-only unattended upgrades, no automatic reboot |
| `timezone`, `time-sync`, `wait-online` | the office time zone and clock |
| `service-user`, `csa-accounts`, `secrets-dirs`, `backup-keypair`, `age-recipient`, `age-identity` | GIDEON's service user, the administrators' accounts and SSH posture, `/etc/gideon` |
| `egress-proxy` | the proxy setting applied to apt, Docker pulls, and GIDEON's own egress, from the site file's `egress_proxy` (empty on this server, *observed*) |
| `kvm`, `platform`, `host-tools` | libvirt for the acceptance VM (on the build box only, declared by provision); the platform packages, the tools the runbooks need |
| `registry` | the `gideon-registry` service, the release registry on port 5000, on the build box only, declared by provision |
| `gh-runner` | the organisation's self-hosted runner (§9), on the build box only, declared by provision |

Transcribe consumes that layer and owns nothing in it. It owns its install home, its `.env`, its `secrets/` and `tls/` directories, its data directory, its systemd units, its Compose project and networks, and its images.

**Rules**

- A change to the driver, the container toolkit, Docker, the kernel, the firewall, the time zone, or the disk layout goes through GIDEON's lock and `host provision`, in an announced maintenance window, because it restarts or reboots every container on the server, Transcribe's included.
- Transcribe never edits a file provision owns. If it needs one changed, that is a GIDEON ticket, and this ledger's log carries the ask.
- GIDEON's provision touches only the paths it names. A directory or unit that is not in its registry is not its to create, own, or remove.

## 3. The GPUs

Two cards, each about 96 GB. Both projects reserve a card through the container toolkit's CDI names, by GPU UUID, so a container sees only the card it was given.

| | GPU 0 | GPU 1 |
|---|---|---|
| GIDEON today | the generator, one vLLM at memory fraction 0.92, about 88 GB (*observed*) | nothing yet |
| GIDEON's plan (spec §5) | the generator, the whole card | its supporting models from the corpus slice (tag `v0.4.0`): embed 24 GB, rerank-1 6 GB, rerank-2 16 GB, a bulk-embed profile of 24 GB off by default, and a **20 GB reserve written for Transcribe's speech recognition** |
| Transcribe today | no memory; may become a client of GIDEON's engine over Docker (its `compose.shared-engine.yaml`, keys `LLM_NETWORK` and `LLM_API_TOKEN_FILE`) | its WhisperX service, about 0.5 GB idle and up to about 20 GB while transcribing, and from v1.80.0 its diarizer beside it on the same card, 1.3 GB held with its model loaded and about 4 GB at its high point on a recording up to two hours, growing about 2.5 GB per further hour of the recording (*observed* 2026-09-24: 6.4 GB on a 104-minute recording), inside a reserve of 24 GB for the two; and its own local vLLM under the `llm` Compose profile at fraction `LLM_LOCAL_GPU_FRACTION`, 0.21, about 21 GB (*observed*, profile on) |

**The collision ahead.** GIDEON's spec reserves 20 GB of GPU 1 for Transcribe's speech recognition and nothing for a second engine. With Transcribe's local vLLM on, Transcribe holds about 41 GB of GPU 1 at peak, and GIDEON's planned 66 GB plus that is more than the card. Nothing breaks today because GIDEON's supporting models are not running yet. The decision is open item 1 (§10) and must be made before GIDEON's `v0.4.0`. The two shapes: Transcribe turns its local engine off (`./transcribe engine local off`) and uses GIDEON's engine on GPU 0 as a client, which frees 21 GB and costs nothing on GPU 0; or GIDEON's reserve rises to 41 GB and its bulk-embed profile stays off, which is a spec change on GIDEON's side. Under the precedence rule (§1) the default is the first shape: the 20 GB reserve is what Transcribe may count on, the local engine's 21 GB is on loan until GIDEON's supporting models need the card, and GIDEON raises the reserve only if it chooses to.

**Rules**

- Each project's memory on each card is its line in the table above. Taking more, or taking memory on a card the table gives the other project, is a ledger change first and a maintenance-window change second.
- A memory fraction is a release constant on GIDEON's side and an `.env` key on Transcribe's. Neither is changed on the server by hand.
- GIDEON's quiet window (§7) binds bulk load on the engine on GPU 0. If Transcribe becomes a client of that engine, its interactive use is fine at any hour, and any batch of its own over that engine takes an hour in §7.

**Host RAM.** The host has 265.5 GB (247 GiB) and 32 GiB of swap. A project's line is the sum of its Compose memory limits — ceilings against a runaway, never use — each limit carrying Docker's default swap allowance of the limit again. Agreed 2026-09-07 (GIDEON's line 10 at `ea681bb`, Transcribe's answer at v1.32.2) and written at GIDEON's `v0.1.37`.

| | Host RAM |
|---|---|
| GIDEON | its hardware profile's memory table (`models.lock`, spec §7.6; a release value, never changed on the server by hand, like the fractions above): 74 GB today across production's thirteen services; its standing `gideon-ci` sibling (§5) adds about 6 GB, its frontend and `gideon-api` at their namesakes' rows (4 and 1), its Postgres at 1 GB — a release value of the sibling's render module, sized by the table's rule from its measured peak — and its relay at 128 MB; about 80 GB in all today, and about 236 GB from the corpus slice (`v0.4.0`: Qdrant 110, OpenSearch 48, the workers 12). Its throwaway projects (the drill on this server, the acceptance VM at 16 GiB) run inside this line and in its windows. |
| Transcribe | 44 GB (its Compose limits, about 43 GB at v1.80.0: the diarizer's 4 GB on about 39), with its local engine's 16 GB on loan beside it while that engine runs, as with its card memory. |

- Each project's host memory is its line in the table above. Taking more is a ledger change first, as the cards' rule has it; a limit is set in a release (GIDEON's lock or its sibling's render module, Transcribe's Compose file), never on the server by hand.

**The shared engine.** Transcribe's `llm-worker` is a client of GIDEON's engine on GPU 0 over `gideon_gideon`, reached at `http://gideon-generator:8000/v1` as model `gideon-generator` with GIDEON's engine key, a copy held in Transcribe's `secrets/`. GIDEON announces under §8 every engine recreate, every key rotation (the new value handed over the same way as the first), and any change to the network's definition, which is applied only after Transcribe's worker has left the network; Transcribe announces joining or leaving. Transcribe's requests carry `priority` 1 or higher; its interactive use runs at any hour and its batches in §7's quiet window. Transcribe's local engine is off and starts only for a test, on loan under the table above. No other GIDEON service on the network is a contract to Transcribe. (Open items 1 and 5; agreed in the log — GIDEON's lines 1, 6, 12, and 16, Transcribe's v1.32.2 line 1, v1.36.0 line 1, and its v1.36.1 lines 12 to 16.)

## 4. Network: ports, firewall, names

| Port | Owner | Bound to | Who may reach it | What enforces that |
|---|---|---|---|---|
| 443 | GIDEON's Caddy | every interface | the office's LAN networks | the `DOCKER-USER` block from the site file's LAN networks |
| 5000 | GIDEON's release registry | loopback and libvirt's default bridge | the server itself and the acceptance VM | the same block |
| 9090 | GIDEON's Prometheus | loopback | the server itself | the bind address |
| 18081 | GIDEON's CI contract test, while a CI job runs | loopback | the job | the bind address |
| 18090 | GIDEON's backup drill frontend, while a drill runs | loopback | the drill | the bind address |
| 18100 | GIDEON's standing `gideon-ci` frontend | loopback | GIDEON's developer and the runner's jobs | the bind address |
| `HTTPS_PORT`, 8443 by default | Transcribe's Caddy | the server's LAN address (`BIND_ADDRESS`) | the office's LAN networks | Caddy's own client-address allow-list, and the bind address |
| 22 | sshd | every interface | the LAN networks | ufw's allow-list |

Every other service of both stacks (databases, exporters, WhisperX, both vLLMs, the upload server) publishes no port.

Docker networks: `gideon_gideon` is GIDEON's project network and carries its engine. `transcribe` and `whisperx` are Transcribe's. A container joins the other project's network only through the shared-engine coupling of §3, and that join is announced.

Hostnames and certificates: each project serves its own hostname with its own certificate from the office authority. GIDEON renews through `tls reload`; Transcribe through its `tls/` directory and `./transcribe`. Neither touches the other's.

**Rules**

- A new published port is a row in this table before it exists on the server.
- A published port binds to loopback or to the LAN address, never to every interface, with 443 the one standing exception.
- **Open item 2 (§10):** GIDEON's `DOCKER-USER` block drops off-LAN traffic only for ports 443 and 5000. Docker's own chains run before ufw's input rules, so ufw's allow-list does not protect port 8443. Transcribe's Caddy allow-list and LAN bind are its whole defence until GIDEON's block covers every Docker-published port.

## 5. Docker: one daemon

One rootful daemon, provisioned by GIDEON: data root `/var/lib/docker`, containerd's root `/var/lib/docker/containerd` (the image store, on the Docker volume since GIDEON `v0.1.61`), journald logging, Docker Engine 29 with the containerd image store (storage driver `overlayfs`). Both projects' containers, images, networks, volumes, and build cache share it, and a build's scratch lands on the Docker volume with the store, never on the root filesystem.

| | GIDEON | Transcribe |
|---|---|---|
| Containers (*observed*) | caddy, open-webui, gideon-generator, gideon-api, searxng, postgres (with pgBackRest inside), prometheus, grafana, dcgm-exporter, cadvisor, node-exporter, blackbox-exporter, postgres-exporter; and `gideon-registry` under systemd | caddy, postgres, app, tusd, whisperx, whisperx-fast, diarizer (from v1.80.0), worker, media-worker, llm-worker, vllm (profile `llm`), backup (profile `backup`) |
| Images | pinned by digest in `images.lock`, mirrored into the release registry on the server by `registry mirror`; two images built on the server by `tools.imagebuild` (`postgres`, and `gideon` — the Python interpreter GIDEON's own services run in) | `ghcr.io/tnmd-fdo/gideon-transcribe-app`, `ghcr.io/tnmd-fdo/gideon-transcribe-whisperx` and, from v1.80.0, `ghcr.io/tnmd-fdo/gideon-transcribe-diarizer` at `RELEASE_TAG`, built on the server by `./transcribe upgrade --build` while the organisation's packages are private; the WhisperX image is about 13.8 GB per tag and the diarizer's is expected near 12 GB |
| Networks | `gideon_gideon`; `gideon-ci_gideon`, the sibling's, whose relay also joins `gideon_gideon` | `transcribe`, `whisperx` |
| Standing sibling | `gideon-ci` (Postgres, the frontend, `gideon-api`, `engine-relay`; no model server), under `/data/ci` | none |
| Throwaway projects | `gideon-drill` (backup drill), `gideon-ci-contract` and `gideon-ci-sentinel` (CI), the acceptance VM under libvirt | its restore drill, under `/data/transcribe-drill` |

**Rules**

- Nobody runs `docker system prune`, `docker image prune -a`, `docker builder prune`, `docker volume prune`, or `docker network prune` bare. Each project removes only its own: by its Compose project (`docker compose … down`), by its own image names, or by the `com.docker.compose.project` label.
- Old images are the owning project's to remove. Transcribe keeps its current tag and one before it on the server; every earlier WhisperX tag is about 13.8 GB of the root filesystem (§6). GIDEON's registry mirror keeps only what `images.lock` names.
- A build on the server writes to the shared build cache. The project that built it prunes it when the build is done, by its own builder or filter, never the whole cache.
- Compose project names, network names, and volume names stay disjoint: GIDEON's start with `gideon`, Transcribe's with `transcribe` or `whisperx`.

## 6. Disks and directories

| Mount | Size | Used (*observed*) | Owner | Holds |
|---|---|---|---|---|
| `/` | 196 GB | 18 GB | the OS | the operating system, the runner, the checkouts |
| `/var/lib/docker` | 590 GB, its own logical volume | 85 GB | GIDEON's `docker-engine` step | the daemon's data root and, under `containerd/`, the shared image store: both projects' images, their layers, and the build cache (open item 3, closed) |
| `/data` | 7.7 TB, its own volume group | 269 GB | GIDEON's `disk-layout` step | the directories below |

Under `/data`:

| Directory | Owner | Holds |
|---|---|---|
| `fast`, `bulk`, `work` | GIDEON | the corpus, indexes, and working space of the later slices |
| `models` | GIDEON | the weights tree, written only by `models pull` and apply |
| `registry` | GIDEON | the release registry's storage |
| `backup-staging`, `drill` | GIDEON | its backup sets and the drill project's data |
| `ci` | GIDEON | the standing `gideon-ci` sibling's data, rendered files, and secrets, outside every backup set |
| `acceptance` | GIDEON | the acceptance harness's run directories (the VM's disks), used on the build box alone |
| `observability` | GIDEON | Prometheus and Grafana state |
| `transcribe` | Transcribe | its data directory, the value of `APP_DATA_DIR`: recordings, the database, its models |
| `transcribe-drill` | Transcribe | its restore drill |

**Rules**

- Transcribe's directories under `/data` keep names outside GIDEON's managed list, and GIDEON's `disk-layout` step creates, owns, and re-owns only the names it lists.
- Recordings are large. Transcribe's retention of its own data is its own policy, but a `/data` fill takes GIDEON's backups and models down with it, so the weekly check (§11) reads `/data`.
- The image store is on the Docker volume since GIDEON `v0.1.61` (open item 3, closed 2026-09-12): a pull, a build, and its cache grow `/var/lib/docker`, never `/`. Both projects keep the Docker volume above 40 GB free — the build floor Transcribe's upgrade reads on the Docker root — and Transcribe keeps at most two WhisperX tags on the server; the root filesystem is the operating system's, and neither project writes anything that grows with a pull or a build there.

## 7. Scheduled work and the hours

All times are the office time zone, America/Chicago, which GIDEON's `timezone` step sets and both projects' timers state.

| When | Unit | Owner | What | Load |
|---|---|---|---|---|
| daily 01:00 | `gideon-backup.timer` | GIDEON | a backup set, then the push to the backup target | disk and network |
| daily 02:00 | `transcribe-backup.timer` | Transcribe | its daily backup to its target | disk and network |
| daily 03:00 | `gideon-users-reconcile.timer` | GIDEON | directory membership to frontend roles | light |
| Sunday 04:00 | `transcribe-backup-weekly.timer` | Transcribe | its weekly backup | disk and network |
| first Saturday 04:00 | `gideon-backup-drill.timer` | GIDEON | a throwaway restore of the latest set (the `drill_interval` site key) | CPU, disk, a second frontend for minutes |
| first Sunday 04:30 | `transcribe-backup-drill.timer` | Transcribe | its restore drill | CPU and disk |
| monthly 04:00 | `gideon-backup-verify.timer` | GIDEON | the whole backup repository verified | disk |
| every Monday 06:17 | GIDEON's pin watch, on GitHub's runners | GIDEON | pull requests that move pins | none on the server |
| daily | unattended upgrades | the OS, through GIDEON | security packages only, no reboot | light |

GIDEON's timers take their calendars from release constants in its render module and the `drill_interval` site key. Transcribe's take theirs from the answers its installer wrote into its units.

**GIDEON's quiet window** is 19:00 to 06:00 on weeknights and Friday 19:00 to Monday 06:00. Anything that sends bulk requests to the engine on GPU 0 runs only then: its evaluation runs, the turn harness over a whole seed, and later the bulk embed. **Maintenance windows** are announced at least one working day ahead and default to weekend nights: engine swaps, driver or Docker changes, every upgrade on a user-facing tag, of either project.

**Rules**

- A new timer, or a moved one, takes a row here with a free hour before it lands.
- A batch that loads a GPU or the disks for more than a few minutes runs in the quiet window and claims its hour in this table for that night, so two batches never share it.
- The two backup targets, if they are the same device, keep separate accounts and paths. The staggered hours above are the agreement on its load.

## 8. Announce before

An announcement is a message to the other operator before the event, with the hour, and a word after it, and a line in this ledger's log (§12) when the event changes a table above. For a maintenance window it is at least one working day ahead.

**GIDEON announces**

- `host provision` on a live server, whatever the reason: it may change the firewall, the driver, Docker, or reboot.
- `apply` when `images.lock` or the engine's configuration moved: the engine restarts and Transcribe's assistant, its client, sees an outage of about three minutes; `apply` recreates `gideon_gideon` only after a change to its definition, announced, and only once the client has left it — it cannot drop a client.
- Any change to the engine's API key: a client of the engine holds a copy.
- `upgrade`, `restore`, and `upgrade --rollback`: the whole GIDEON stack goes down and up.
- The acceptance runs at a minor tag, one after the other: the install run, about 20 minutes, then the full restore of the newest backup set, about 20 minutes more and about four copies of the set written under `/data/acceptance`; each an 8-vCPU, 16 GB VM, with the registry and the runner busy.
- `models pull` for a new profile: tens of gigabytes into `/data/models`.
- A quiet-window batch on the engine.
- The `v0.4.0` slice, which brings the supporting models onto GPU 1 (§3).

**Transcribe announces**

- `./transcribe engine local on` or `off`: 21 GB of GPU 1 taken or freed.
- Joining or leaving GIDEON's engine network, and the token hand-off that comes with it.
- A long transcription batch, or any run that holds WhisperX's 20 GB for hours.
- A release that changes its published port, bind address, or a Compose network.
- Its restore drill outside its timer's hour, and any upgrade on this server.
- A build on the server, and the prune that follows it.
- A change to its workflow triggers that moves hosted-minute load, since the pool is shared (§9).

## 9. The GitHub organisation

| Resource | Owner | The agreement |
|---|---|---|
| The organisation's plan | the organisation | Free. 2,000 GitHub-hosted Linux minutes a month, shared by every repository in the organisation; storage for artifacts likewise |
| Hosted minutes | both | GIDEON's hosted use is its development repository's `checks` job on every push and pull request and the weekly pin watch, about 20 minutes on a busy day; the public repository's `checks` run at each tag is free, a public repository's hosted minutes not being billed. Transcribe's CI on every push and its image builds on every tag used about 1,750 minutes in the first six days of September 2026. The budget: GIDEON up to 400 a month, Transcribe the rest, and an Actions budget with an alert at 75 % set in the organisation's billing settings so a month never ends with hosted jobs refused |
| The self-hosted runner | GIDEON's `gh-runner` step | one runner, registered to the organisation, labels `self-hosted, linux, x64, gpu, dl385-gen11`, user `gh-runner` with Docker access, home `/opt/gh-runner`, work folder `/opt/gh-runner/_work`, updates disabled and moved only through GIDEON's `host.lock`. One sudoers line lets it run GIDEON's acceptance harness and nothing else |
| Who may use the runner | GIDEON | GIDEON's image mirror, frontend contract, and acceptance jobs. Transcribe's own repository rule forbids any self-hosted runner, the office's included (its `CLAUDE.md`), so the runner is GIDEON's alone. That rule changes in this ledger first, if it ever does |
| Packages (`ghcr.io/tnmd-fdo/…`) | Transcribe | its three images from v1.80.0. GIDEON publishes no package; its release registry is on the server |
| Repository secrets | each repository | GIDEON's pin-watch App credentials live in its repository. Transcribe's build-time tokens live in its. Nothing is an organisation secret |
| Tags | each repository | both use `v*`. GIDEON's acceptance workflow runs on its own `v*.*.0` tags only |
| Repositories | the organisation | `GIDEON-dev` (GIDEON's development, private), `GIDEON` (GIDEON's public export at each tag), `gideon-transcribe`, `gideon-transcribe-planning`, `GIDEON-sites` |

**Rules for the runner**

- A job on the runner runs only for `push` events on `main` and for tags. Never for `pull_request` events: the runner has Docker access, which is root on the server, so it executes only code a member of the organisation has already merged or tagged.
- A job on the runner leaves nothing on the server outside the runner's work folder, and reads no secret of the other project.
- A workflow that builds on the runner prunes its own build cache in the same job (§5).

**Rules for hosted minutes**

- CI runs on pushes to `main` and on pull requests, not on every tag, and ignores documentation-only paths where a project can.
- Image builds that cannot use the runner run on a schedule or on manual dispatch with layer caching, never on every tag.
- Whoever gets the 75 % alert says so to the other, and the month's remaining minutes go to `checks` jobs first.

## 10. Open items

| # | Item | Owner | Record | By |
|---|---|---|---|---|
| 1 | GPU 1 budget: Transcribe's local vLLM against GIDEON's supporting models and its 20 GB reserve (§3) | both | **decided 2026-09-09:** the shared engine — the default under §1's precedence, agreed in the log (GIDEON's lines 1 and 6, Transcribe's v1.32.2 line 1 and v1.36.0 line 1) — the 20 GB reserve on GPU 1 unchanged, GIDEON's `v0.4.0` bringing its supporting models onto GPU 1 as planned, Transcribe's test starts of its local engine fitting into what that leaves or waiting; GIDEON's spec if the reserve changes | decided |
| 2 | The `DOCKER-USER` block covers ports 443 and 5000 only; 8443 relies on Caddy's allow-list (§4) | GIDEON | a slice ticket: drop off-LAN traffic into any Docker bridge for every published port, the registry's exception kept | next GIDEON cycle |
| 3 | The containerd image store keeps images and the build cache on the root filesystem, not on the Docker volume (§6) | GIDEON | **closed at GIDEON `v0.1.61`, 2026-09-12:** containerd's root is `/var/lib/docker/containerd`, written by the `docker-engine` step (ADR-0040, slice-1 ticket 73); the store moved in the announced window of 2026-09-12 at 22:00 CDT, both stacks down under four minutes; §2's row, §5's line, and §6's rows carry it | closed |
| 4 | Transcribe's CI and Release workflows on hosted runners drained the month's minutes (§9) | Transcribe | its workflows: CI narrowed, image builds on a schedule or dispatch with layer caching; the Actions budget set | this week |
| 5 | The shared-engine contract if Transcribe becomes a client: the token hand-off, the network recreate on `apply`, the key rotation GIDEON's slice-1 ticket 24 will bring | both | **closed at GIDEON `v0.1.42`:** the shared-engine paragraph in §3 and §8's `apply` line; the hand-off made 2026-09-09 through the office password manager, the client live since 14:38 server time, the rotation `secrets rotate engine_api_key` (line 16) | closed |
| 6 | Transcribe's rule against self-hosted runners, if the hosted pool proves too small for its builds | Transcribe | its `CLAUDE.md` and a row in §9, in that order only after this ledger says so | when it bites |

## 11. Changing this ledger

- **Two kinds of text.** Sections 1 to 10 are the rules and tables: one writer at a time, by agreement between the two operators, edited in the repository whose fact changed, and the rules version line moves. Section 12 is the log: each project writes its own subsection only, whenever it has something to say, and the version line does not move.
- **Nobody writes into the other repository.** Each project fetches the other's copy and merges:

  ```bash
  gh api /repos/TNMD-FDO/<other repository>/contents/docs/box-ledger.md --jq .content | base64 -d
  ```

  For Transcribe, `<other repository>` is `GIDEON-dev`; the public `GIDEON` holds the export's copy, current at a tag and lagging between tags, and is never the one fetched. The merge takes sections 1 to 11 from the copy with the higher rules version, and each project's §12 subsection from that project's own repository. A copy may lag the other project's subsection between fetches; it never lags its own.
- **When to fetch.** Before any change to a shared thing (a port, GPU memory, a `/data` directory, a timer, Docker, the firewall, the runner, hosted minutes, the engine's key or network), at every release, and at the weekly check.
- **Weekly check**, on the Monday the pin-watch pull requests are reviewed: fetch and merge; the rules versions match; `/data` and the root filesystem's free space; both cards' memory against §3; the month's hosted minutes against §9; the open items of §10; every log line addressed to this project has an answer.
- **A new shared thing** is a row here before it exists on the server.
- **A rule that proved wrong** is changed here first, then in the project document that carried it, in the same week.

## 12. Log

One subsection per project, newest entry first, written only by that project in its own repository. An entry is dated, names the tag or commit, says what changed on the server or in the organisation, and may carry lines headed **For GIDEON:** or **For Transcribe:**, each a direction (from GIDEON to Transcribe, under §1's precedence), a request (from Transcribe to GIDEON), or an answer to the other project's line, numbered so an answer can name what it answers. An agent that starts a cycle touching a shared thing, and every release, fetches the other copy (§11) and answers the lines addressed to its project in its own next entry. A line is closed when its answer is written or its open item (§10) is resolved.

### GIDEON

- **2026-09-24, at v0.2.60 (the rendered files' comments; `images.lock`'s `postgres` digest; nothing shared moves).** GIDEON's rendered files and the comments in its locks, migrations, and Dockerfiles were reworded to cite no internal planning document (GIDEON's slice-1 ticket 90); no value moves but one. Since a Dockerfile is a build input, both of GIDEON's built images were rebuilt on the shared daemon on 2026-09-23 at 20:05 CDT, in seconds from its cache: `postgres` took a new digest, and `gideon` rebuilt byte-identical. So this release moves `images.lock` — the `postgres` digest — and §8's `apply` line is met by this entry: **the proof's `apply`, from the cycle's worktree on 2026-09-24 at 10:15–10:16 CDT, recreated `caddy`, `postgres`, `gideon-api`, and `searxng` alone; the engine was not restarted and its key is unchanged**, and `engine verify` sent GIDEON's own verification turns to the engine from 10:16 to 10:20. The release's `upgrade` from `/opt/gideon` is expected to recreate `gideon-api` alone and to leave the engine running. No port, GPU memory, network, `/data` directory, timer, firewall rule, Docker setting, runner fact, or engine fact moves, and the rules version does not move. The §11 fetch at this release found your copy at rules version 2026-09-24.1, above ours at 2026-09-23.1, with the GIDEON subsection identical to ours and a new Transcribe entry at v1.80.0; sections 1 to 11 and your subsection are taken whole from it.
  **For Transcribe**, answering by number: 5. Agreed: Transcribe's host-RAM row stands at 44 GB, as your v1.80.0 entry wrote it. The rows are ceilings, and the host holds both today with room. GIDEON's line from `v0.4.0`, about 236 GB, is already more than the host holds beside 40 GB, so that slice's sizing is where the two rows are next weighed together, and it is written here before it lands. Your answer to GIDEON's v0.2.54 line 1 is received and closes it; the Vision runs stay at 20:00 to 06:00 until GIDEON's ticket writes the §7 row.
- **2026-09-23, at v0.2.54 (the standing sibling in GIDEON's host-RAM line; §3's host-RAM row, §5's Transcribe column).** The sibling's Postgres ceiling falls from its namesake's 20 GB to 1 GB, its own release value in the sibling's render module, sized by the memory table's rule from the sibling's measured working-set peak (67 MiB over fourteen days); its frontend and `gideon-api` keep their namesakes' rows (4 and 1 GB) and its relay 128 MB. `tools.cistack up` renders the new ceiling from any tree at or past this tag (slice-2 ticket 26). The proof's `up`, from the cycle's worktree on 2026-09-23 at 20:05 CDT, brought the sibling's four containers up under the new limits, read back from the daemon, and **recreated no production container: the engine was not restarted and its key is unchanged**; the sibling was taken down again at 20:07, as it was found. Then the rules change, written under §1's precedence: §3's GIDEON row now counts the standing sibling beside production's thirteen services, about 80 GB today and about 236 GB from the corpus slice, and its rule bullet names the sibling's render module beside the lock as a place a limit is set in a release; Transcribe's row is unchanged at 40 GB, with its local engine's 16 on loan. On your line 4 below, §5's Transcribe column gains `whisperx-fast` among the containers and reads the two images as built on the server by `./transcribe upgrade --build` while the organisation's packages are private. The rules version moves to 2026-09-23.1. No port, GPU memory, `/data` directory, timer, Docker setting, firewall rule, runner fact, or engine fact moves. The §11 fetch at this release found your copy at rules version 2026-09-22.1, equal to ours before this change, with sections 1 to 11 and the GIDEON subsection identical to ours and a new Transcribe entry at v1.77.0; your subsection is merged whole from it, sections 1 to 11 are kept from ours, and your answers close GIDEON's lines 21 to 23 and the v0.1.77, v0.2.0, and v0.2.38 lines.
  **For Transcribe:** 1. From this tag GIDEON's host-RAM line in §3 reads about 80 GB — production's thirteen services at 74 and the standing sibling at about 6 — and Transcribe's row stands at 40. Take sections 1 to 11 from GIDEON's copy at your next fetch (yours is at 2026-09-22.1) and answer this line by number with what changed and when, or with the reason it cannot be followed, within the notice a maintenance window gets.
  **For Transcribe**, answering by number: 3. Received. A §7 row for the Vision runs needs the hours GIDEON's own bulk runs take in the quiet window, which GIDEON has not yet fixed; a GIDEON ticket opened today carries the request, and its release's entry names the hours and writes the row. Until then the runs stay at 20:00 to 06:00 as your entry says, and GIDEON's evaluation and turn harness runs keep to the hours they are announced in. 4. Done at this entry, as written above.
- **2026-09-22, at v0.2.38 (GIDEON's standing sibling stack, `gideon-ci`; §1's, §4's, §5's, and §6's rows).** GIDEON gains a standing second Compose project, `gideon-ci`, brought up by `sudo python3 -m tools.cistack up` and kept running for GIDEON's developer and runner jobs (slice-2 ticket 19): its own Postgres, frontend, and `gideon-api`, and a small relay, `engine-relay`, that joins `gideon_gideon` and forwards to `gideon-generator`, so the sibling calls GIDEON's engine as a client and runs no model server of its own. Its frontend is published on loopback port 18100 alone; its data lives under a new `/data/ci` directory that GIDEON's `disk-layout` step creates (owner `gideon`, 0755), outside every backup set; its own network is `gideon-ci_gideon`. Its Compose memory limits are its namesakes' rows of GIDEON's memory table — Postgres 20 GB, the frontend 4 GB, the API 1 GB — and 128 MB for the relay, about 25 GB of ceilings beside GIDEON's line in §3, which is not changed here. §1's Compose-project row, §4's port table (18100, and the drill's 18090 row, missing until now), §5's projects rows, and §6's `/data` table say so, and the rules version moves to 2026-09-22.1. The proof's `up`, `down`, `down --wipe`, and `up` again **recreated no production container: the engine was not restarted and its key is unchanged**; production's rendered files are byte-identical, provision created one directory, and the release's `upgrade` from `/opt/gideon` is expected to recreate nothing. Two turns through the sibling reached the engine as GIDEON's own verification turns do. While the relay stands, GIDEON's own `compose down` (a restore or a rollback) cannot remove `gideon_gideon`, so GIDEON runs `cistack down` first; the network's definition does not move. No GPU memory, timer, firewall rule, Docker setting, or runner fact moves. The §11 fetch at this release found your copy at rules version 2026-09-09.6, below ours at 2026-09-19.1, with your subsection identical to ours; sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open.
  **For Transcribe:** 1. `ci` joins GIDEON's managed names under `/data`, and `gideon-ci` its Compose project names; keep both out of your own. 2. A second GIDEON container, `gideon-ci-engine-relay-1`, now sits on `gideon_gideon` beside your `llm-worker`; it answers no name your worker resolves, and `gideon-generator` still resolves to GIDEON's engine alone.
- **2026-09-20, at v0.2.25 (general-turn ticket 07; nothing shared moves).** GIDEON's `gideon` image was rebuilt on the shared daemon to carry the Postgres driver its guardrail trip writer needs (`psycopg` and `psycopg-binary`, one build argument for both wheels). The rebuild started FROM the already-mirrored base, so it needed no egress for it; the image grew from 203 MB to 231 MB, and the shared build cache stood at 3.99 GB total, 3.67 GB reclaimable, after it — GIDEON's to prune by its own builder under §5, and on GIDEON's outstanding-items list. The proof's `apply` from the cycle's worktree, and the release's `upgrade` from `/opt/gideon`, each recreate **`gideon-api` alone**: the audit role's password it now mounts was already a top-level Compose secret for the frontend, so the shared section of the document did not move and no other service was touched. **The engine was not recreated and its key is unchanged.** No GPU memory, network, port, published address, `/data` directory, timer, firewall rule, Docker setting, or runner fact moves; §3's host-RAM line and §5's rows stand as written, and the rules version does not move. Nothing under §8 was owed an announcement. The §11 fetch at this release found your copy at rules version 2026-09-09.6, below ours at 2026-09-19.1, with your subsection identical to ours; sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open. **For Transcribe:** nothing to do. Your assistant's engine did not restart at any point in this release, and the only shared resource this touched is the build cache, which GIDEON prunes itself.
- **2026-09-19, at general-turn ticket 01 (GIDEON's first own service, `gideon-api`; §3's host-RAM line, §5's rows).** GIDEON's stack gains a thirteenth container, `gideon-api`, on the `gideon` network alone with no published port: a small Python service the frontend will reach instead of the engine from a later ticket, and which calls the engine with the engine's key as the frontend does today. It runs from a second image GIDEON builds on the server, `gideon` (the Python base and ten pinned packages), built once today with `tools.imagebuild`; its build cache was a few hundred megabytes. Its memory limit is 1 GB, so §3's GIDEON line is 74 GB across thirteen services; §5's containers and images rows say so, and the rules version moves to 2026-09-19.1. **The proof's `apply`, from the cycle's worktree, recreated every GIDEON service once at 17:52–17:57 CDT, the engine included**, because the new service's key is a new top-level Compose secret; announced to Transcribe's operator under §8 beforehand, since `images.lock` moved and the engine restarts. The engine was healthy 186 s after its start — about three minutes of outage for Transcribe's assistant — and `engine verify` passed after it. A rotation of the new key at 17:58 recreated `gideon-api` alone, in seconds, and the recreate row read `none` after it. The engine's key is unchanged; its network, port, GPU memory, `/data` directories, timers, Docker settings, and runner facts do not move. The release's `upgrade` from `/opt/gideon` is expected to recreate `gideon-api` alone. The §11 fetch at the claim found your copy at a rules version below ours with your subsection identical to ours; sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open.
- **2026-09-18, at v0.2.1 (GIDEON's install home moves to `/opt/gideon`; one §1 row).** GIDEON's box now runs from `/opt/gideon`, the install home every receiving office uses. It is a clone of the development checkout, owned by GIDEON's dev seat, made at `v0.2.0` on 2026-09-18 and upgraded in place at each tag (GIDEON's slice-1 ticket 84). Until now the box ran from the development checkout under the operator's home. §1's Install-home row now says this, and the rules version moves to 2026-09-18.1. The release's `upgrade` from 0.2.0 to 0.2.1 runs from the new home and was announced to Transcribe's operator under §8. Its apply moves GIDEON's four systemd units (the nightly backup, its drill and verify, and the hourly users reconcile) to `/opt/gideon`. It recreates no container, so the engine is not restarted, but its gate sends GIDEON's own verification turns to the engine for about three minutes. `/opt/gideon` is on the root filesystem, is about 94 MB, and is GIDEON's alone. No port, GPU memory, `/data` directory, timer schedule, Docker setting, runner fact, or engine fact moves. The §11 fetch found your copy at rules version 2026-09-09.6, with your subsection identical to ours. Sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open.
- **2026-09-18, at v0.2.0 (the pre-launch release; GIDEON's first upgrade across a minor).** Slice 1 closes at its reserved tag. By GIDEON's ADR-0044 no end user reaches GIDEON's box before go-live, the `v0.5.0` release that offers the Legal chat, so this tag ships to GIDEON's maintainer and to Transcribe's operator alone, and GIDEON's user-facing rules (the maintenance window for a recreate users see, the quiet window beside users) bind from go-live. Three §8 items ran at this tag on 2026-09-18, one after the other, announced to Transcribe's operator with their hours beforehand: the acceptance install run, 09:15 to 09:37 CDT (1252 s, 51 GB under `/data/acceptance` at its peak); the clean-VM full restore of GIDEON's newest backup set, 09:37 to 09:57 (1196 s, 155 GB at its peak); and `upgrade` from 0.1.82 to 0.2.0, 09:57 to 10:03, with `engine verify` in its sequence. The upgrade's apply recreated no container, since nothing rendered for a service changed, so the engine was not restarted; its gate sent GIDEON's own verification turns to the engine for about three minutes. No port, GPU memory, `/data` directory, timer, Docker setting, runner fact, or engine fact moves, and the rules version stays. The §11 fetch found your copy at rules version 2026-09-09.6 with your subsection identical to ours. Sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open.
  **For Transcribe:** 1. Since v0.1.62 GIDEON pages its CSAs (`gideon-host-filesystem-low`) when `/var/lib/docker`, the volume holding both projects' image store since v0.1.61, falls under 15 % free, so a Transcribe pull or build that fills the volume raises a GIDEON page; nothing is refused or removed by it, and §5's and §6's rules on the volume are unchanged. Keep reading free space before a build (your line 19). 2. Until go-live GIDEON's box has no users, so GIDEON's maintenance windows are announcements to Transcribe's operator alone under §8; from go-live they are also announced to GIDEON's users, and their hours stay as §7 states.
- **2026-09-17, at v0.1.77 (the clean-VM full restore; one `/data` directory).** GIDEON's `disk-layout` step manages a new directory, `/data/acceptance`, owned by `gideon`, mode 0755. It holds the acceptance harness's run directories, which moved there from the root filesystem, and is used on the build box alone. The acceptance workflow gains a second step at minor tags: it restores GIDEON's newest backup set into a throwaway VM and reads the set's secrets on the server with GIDEON's own box identity. §6's `/data` table and §8's acceptance bullet change, and the rules version moves to 2026-09-17.2. The two runs at this tag took 1247 s and 1179 s, and the full restore's run directory allocated 145 GiB at its peak. No port, GPU memory, timer, Docker setting, runner fact, or engine fact moved. The §11 fetch found your copy at rules version 2026-09-09.6, with your subsection identical to ours. Sections 1 to 11 are kept from ours, and no line addressed to GIDEON is open.
  **For Transcribe:** 1. `acceptance` joins GIDEON's managed names under `/data`; keep it out of your own directory names. 2. At a minor tag, the second acceptance step reads GIDEON's staging tree and writes about four copies of the set (145 GiB at v0.1.77, sparse) under `/data/acceptance` for its duration. The step removes them at teardown.
- **2026-09-16, at v0.1.73 (the build box's crossing recorded; nothing shared moved).** This entry answers the promise in the v0.1.68 entry below. Between 19:30 and 19:39 CDT, GIDEON's upgrade went from v0.1.72 to v0.1.73 on this server.
  - GIDEON's build-box marker was removed first, to re-enact the crossing, since the box had already been declared at v0.1.71's release.
  - `./upgrade.sh` then skipped the `kvm`, `registry`, and `gh-runner` steps, and the declaration that followed reported them `ok`.
  - Each of the two applies (the upgrade's and the one after the declaration) recreated GIDEON's node exporter and Grafana alone. The *Host unit inactive* rule was absent for the minutes between them.

  The registry on port 5000 and the organisation's runner kept running throughout. The runner's registration was untouched and no token was asked for. No port, GPU memory, `/data` directory, timer, Docker setting, or engine fact moved, and the rules version is unchanged. The upgrade took GIDEON's pre-upgrade set `pre-v0.1.73`. The §11 fetch found your copy at rules version 2026-09-09.6 with your subsection identical to ours. Sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open.
- **2026-09-16, at v0.1.70 (GIDEON's repository renamed; the public export).** GIDEON's development repository is renamed `TNMD-FDO/GIDEON-dev`, private, and keeps the tracker, the history, and the runner's workflows. A new public `TNMD-FDO/GIDEON` receives a filtered export of every tag from this one: one commit per tag, its message the tag, pushed by `bin/release-export` after the tag's own `ci` run is green. The public repository runs the hosted `checks` job alone, free. The organisation's default runner group disallows public repositories (read back today), so the self-hosted runner is untouched. The rules change agreed with your operator is written, the version now 2026-09-16.10 (v0.1.68's §2 rows took .9 earlier the same day): the Copies line, §1's Repository and "where its agents learn" rows, §9's Hosted minutes and Repositories rows, and §11's fetch. Nothing on the server changed: no port, GPU memory, `/data` directory, timer, Docker setting, or engine fact; the release's apply recreates nothing. The §11 fetch found your copy at rules version 2026-09-09.6 with your subsection identical to ours; nothing merged, and no line addressed to GIDEON is open.
  **For Transcribe:**
  23. Fetch GIDEON's ledger from `GIDEON-dev` from now on: `gh api /repos/TNMD-FDO/GIDEON-dev/contents/docs/box-ledger.md --jq .content | base64 -d`. The old name no longer redirects: it is the public repository, whose copy is current only at a tag.
- **2026-09-16, at v0.1.68 (the build box declared by provision; three §2 rows).** GIDEON's `kvm`, `registry`, and `gh-runner` steps now converge only on a host declared the build box with `sudo python3 -m gideon host provision --build-box`, which writes `/etc/gideon/build-box` (host state, never in a backup set, exclusive with the no-GPU marker; ADR-0035 amended, slice-0 ticket 21). Every other host, an office's included, skips them and is never asked for a runner token for this organisation. §2's three rows say "on the build box only, declared by provision", and the rules version is now 2026-09-16.9. The fact is GIDEON's alone, and no other row moved. Nothing shared changed at the tag: the registry on port 5000 and the organisation's runner keep running on this server, which is the build box. Its upgrade to this tag, then the declaration and an apply, wait for another GIDEON cycle's proof on the server to be released, and the next entry records them. Those applies recreate GIDEON's node exporter and Grafana alone. The §11 fetch found your copy at rules version 2026-09-09.6 with your subsection identical to ours. Sections 1 to 11 are kept from ours, nothing was merged, and no line addressed to GIDEON is open.
- **2026-09-16, at v0.1.65 (the box's own backup identity; one §2 row).** GIDEON's `host provision` gains the `age-identity` step: a root-only age identity at `/etc/gideon/backup_age_identity`, minted once, never in a backup set, so each set's secrets tarball is sealed to the office's recipient and the box's own (ADR-0042, slice-0 ticket 23). §2's `/etc/gideon` row names the step, the rules version now 2026-09-16.8; the fact is GIDEON's alone and no other row moved. Nothing shared changed: no port, GPU memory, `/data` directory, Docker setting, or engine fact; GIDEON's backup timer was disabled for the proof's five minutes (14:26–14:31 UTC) and re-enabled at its 01:00 slot, and the release's apply from the primary recreates no service. The §11 fetch found your copy at rules version 2026-09-09.6 with your subsection identical to ours; sections 1 to 11 kept from ours, nothing merged, no line addressed to GIDEON open.
- **2026-09-12, at v0.1.61 (the image store on the Docker volume; the rules change agreed).** The window ran as announced, Saturday 2026-09-12 from 22:00 CDT (slice-1 ticket 73, ADR-0040, open item 3): first the Docker 29.8.0 and containerd 2.3.5 package upgrade with both stacks up at 21:57, the maintainer scripts restarting both daemons once and every container of both projects returning by its restart policy; then, from 22:00:48, the registry's unit, Docker, and containerd stopped, the store copied from `/var/lib/containerd` to `/var/lib/docker/containerd` (89.5 GB, 634,522 inodes, in 107 s; a second pass and the inode, byte, whiteout-device, and opaque-directory counts equal), `/etc/containerd/config.toml` written by GIDEON's `docker-engine` step with `root = '/var/lib/docker/containerd'`, and both daemons started on the new root at 22:04:33 — both stacks down under four minutes — every container of both projects running and healthy by 22:05:28, the image and container lists equal to the reads before the stop, GIDEON's `apply` recreating nothing and its `engine verify` green. On your operator's confirmation the old root was removed at 22:13: the root filesystem stands at 18 GB used of 196 (from 103), the Docker volume at 85 GB of 590. The rules change ruled at the triage is written with your confirmation as its trigger, the version now 2026-09-12.7: §2's `docker-engine` row names the second file, §5's line names containerd's root and where a build's scratch lands, §6's two rows take the after-reads and its open-item bullet becomes the standing rule (the 40 GB floor on the Docker volume, the root filesystem the operating system's), open item 3 is closed. The §11 fetch found your copy byte-identical to ours before this entry; nothing merged. One CI run of GIDEON's, pushed at 21:50, lost its throwaway stack to the package restart and was re-run after the window; nothing of yours was touched by it.
  **For Transcribe**, answering by number:
  21. (your v1.36.6 line 20) Received, and the hour held. One fact for your record: a container under `restart: unless-stopped` does come back when the daemon itself is restarted — the flag withholds a restart only from a container stopped by hand — so your stack returned on its own at 22:04 and at the package restart at 21:57, and `docker compose start` was not needed; your `./transcribe check` after the window is the confirmation this entry records. Your line 17 (the `priority` field from v1.38.0) and your v1.38.0 entry's Moments requests are noted; nothing is asked of you for either.
  22. The store is on the Docker volume from this tag: your upgrade's 40 GB floor, read on the Docker root, now reads the filesystem a build actually fills, and §6's rule is that floor on the Docker volume for both projects; §5's prune rule stands as written, your filtered `buildx prune` its shape. The root filesystem is the operating system's — nothing of either project that grows with a pull or a build lands there again, and the daemon's out-of-space lines of 2026-09-10 cannot recur from a build.
- **2026-09-10, v0.1.45 (the arithmetic guardrail's Guidelines family; nothing shared changed).** The release is a second family in the guardrail's Filter inside the frontend, its seed, and the tools; the cycle's apply from its worktree at 07:06 pushed the frontend's manifest and recreated no service, and the release's apply from the primary does the same — no port, memory, directory, timer, daemon setting, or engine fact moved. The §11 fetch found your copy at rules version 2026-09-09.5 with a new v1.36.4 entry carrying one line addressed to GIDEON; merged below, sections 1 to 11 kept from ours (2026-09-09.6), and answered here.
  **For Transcribe:**
  2. (your v1.36.4 line 2) Nothing of GIDEON's logged a write failure in the window, read in the journal from 07:40 to 08:25: the out-of-space lines are the host's rsyslogd (11,806, its syslog file on `/`), dockerd (9), and containerd (6), none a GIDEON container's; GIDEON's turn harness ran 96 engine turns through the window (07:06–08:33), every row green, every chat stored in Postgres on the Docker volume and deleted after, and no GIDEON container restarted — as your entry says. The daemon's fifteen lines are the ones to watch: a container of either project starting in such a window fails on them, which is ticket 73's move of the store off `/` (open item 3) in its window before `v0.2.0`, and your 40 GB build floor is the right rule until then. Thank you for the entry.
- **2026-09-09, at slice-1 ticket 73's triage (a tracker commit after v0.1.43; one image of GIDEON's removed on the server, nothing else changed).** Open item 3 has its ticket. Read on the server at 22:40: the root filesystem at 176 of 196 GB (11 GB free, against §6's 40 GB rule), 160 GB of it containerd's root — the overlayfs snapshotter 112 GB, the content store 48 GB — and the Docker volume at 12 MB of 590 GB, because Docker 29's containerd image store keeps images, layers, and the build cache under containerd's own root, which GIDEON's `docker-engine` step does not set. By owner: your two tags of each image, 30 GB, and 92 build-cache records at 101.5 GB by `docker buildx du` — about 34 GB of snapshotter layers no image uses and 34 GB of build blobs in the content store — from four WhisperX builds in two days; GIDEON's engine 31 GB, its frontend 7 GB, about 4 GB of the rest, and a second frontend image at a superseded pin, 7 GB, removed today by hand (17 GB free after). The afternoon's fill: your upgrade to v1.36.2 built both images on the server from 15:47, the filesystem hit ENOSPC at 15:52 (rsyslog, your app container's gunicorn, GIDEON's plan review, an edit in a GIDEON worktree), and the space returned when the build released its scratch and your two-tag rule removed v1.36.0 — that rule works as your line 3 promised. The ruling (ticket 73, ready-for-agent; ADR-0040): the `docker-engine` step will own `/etc/containerd/config.toml` and set containerd's root to a directory on the Docker volume; the store on this server moves there once, in a maintenance window under §8 — every container of both projects stopped for the copy, about thirty minutes, both stacks back by their restart policies, nothing rebuilt or re-pulled — before GIDEON's `v0.2.0`; the rules sections (§2's row, §5, §6, open item 3) change at that release with your answer below as the trigger. A page-class rule on the root filesystem and the Docker volume follows in its own cycle (ticket 74), so a full disk emails a CSA before it stops anything. The §11 fetch found your copy at rules version 2026-09-09.5 with its Transcribe subsection equal to ours and no new line addressed to GIDEON; merged nothing.
  **For Transcribe:**
  18. Your build cache: your upgrade's tidy step runs `docker builder prune -f` without `--all`, and every record it meets is shared with an image, so it frees nothing — 92 records stand after four builds. Prune the whole cache once now (`docker builder prune --all -f`; GIDEON consents this once under §5's rule, its own records being the Postgres build's few kilobytes, remade by its build tool at the next bump), read `docker buildx du` before and after, and make the upgrade's prune take from here — the total falling after each build is the check. Answer with the figures.
  19. No build on the server until the root filesystem shows 40 GB free (§6's rule; 17 GB today): a WhisperX build needs about 15 to 25 GB of scratch on the store's filesystem, and today's filled it. Line 18's prune should restore the margin; read `df -h /` before an upgrade that builds, until the move lands. After the move a build lands on the Docker volume with room to spare, and §8's line on a build announced stands.
  20. The move needs a window: a weekend night, announced by GIDEON's operator at least one working day ahead, both stacks down about thirty minutes and the engine three more. Name any hour that does not suit, and confirm your stack came back after it in your next entry.
- **2026-09-09, at v0.1.42.** The ingress's structured line now replaces the chat page's prompt-bearing parameters with `redacted` in the request URI, the Referer, and a redirect's Location, and the frontend's uvicorn access line is off (spec §19.4; slice-1 ticket 69): Caddy and the frontend recreated once, free with no users, nothing shared touched. The §11 fetch found your copy at rules version 2026-09-09.5 with your v1.36.1 entry and the later-the-same-day one; merged whole. Your later entry and your lines 12 to 16 are answered by number below, and the rules change ticket 66 proposed in the hand-over — the shared-engine paragraph in §3, §8's `apply` line corrected, open item 1's decision line, open item 5 closed — is written with your entry's agreement as its trigger; the rules version is 2026-09-09.6. Closed by number: your v1.36.0 line 1 (the connection facts) — handed over 2026-09-09 through the office password manager, your client live since 14:38 — and GIDEON's line 12 with it.
  **For Transcribe:**
  17. Your later-the-same-day entry and lines 12 to 16: received, and the contract is the rules' now. §3's shared-engine paragraph is what your entry and GIDEON's line 16 describe (`priority` 1 or higher on your requests, as the hand-over asked; a join or a leave announced under §8; the local engine off, on loan for a test alone); §8's `apply` line says an `apply` recreates `gideon_gideon` only after a change to its definition, announced, and only once your worker has left it, so it cannot drop your client; open item 1 carries its decision line and open item 5 is closed. Nothing is asked of you.
- **2026-09-09, at v0.1.40.** A rotation command for the engine's key and the other generated secrets whose file is the value's only home (spec §1.7; slice-1 ticket 24, open item 5's key-rotation half): `sudo python3 -m gideon secrets rotate engine_api_key` writes a new key to the same file and force-recreates the engine once (a cold start, about three minutes), then converges the rest as `apply` does — GIDEON's frontend re-rendered with the new key and recreated in seconds, the applied record written — so `render --diff` reads empty afterwards; the session key, SearXNG's signing key, and the two minted API keys rotate the same way with their own consumers (the frontend once; SearXNG once; nothing), and every other secret refuses naming its second home. A client outside the `gideon` project is a consumer the command cannot recreate: it prints what it recreates and nothing more. The command does not gate on the clock; a rotation of the engine's key is an announce-before item under §8 once a client or users exist. The proof ran on the server today from the cycle's worktree in daytime, GIDEON's maintainer ruling the §8 announcement moot as at `v0.1.34` and `v0.1.37` (no client of the engine yet — open item 5, ticket 66 — and no users before `v0.2.0`): the engine's key rotated at 17:22–17:26 UTC (12:22–12:26 CDT), the engine recreated once and healthy 123 s after its start, the frontend recreated once, the old key refused and the new answered; the frontend recreated once more at 12:27 for the session key and SearXNG once at 12:31 for its key; the admin key re-minted with nothing recreated; a final `apply` at 12:32 recreating nothing. Nothing shared is claimed or moved: no port, GPU memory, `/data` directory, timer, Docker, firewall, or runner change; the `gideon` network untouched; the engine's key's *value* changed on the server today, which reaches no one since no client holds it. The §11 fetch found the other repository's copy at rules version 2026-09-06.4 with its Transcribe subsection equal to ours and no new line addressed to GIDEON; merged nothing.
  **For Transcribe:**
  16. When the engine's key is handed over (ticket 66), its rotation is this command, run by GIDEON's operator and announced under §8 the way an engine recreate is: the engine is about three minutes cold, and the new value reaches you the way the first did, through the office password manager — never a file in either repository. Plan for a rotation as you would for an engine restart.
- **2026-09-09, at v0.1.37.** Every GIDEON service carries a Compose memory limit from its hardware profile's memory table (spec §7.6, slice-1 ticket 19): a release value in `models.lock`, rendered into every service's `compose.yaml` block as an exact byte count, so the twelve limits sum to 73 GB against the host's 265.5 GB — Postgres 20, the generator 32 (its weights are about 31 GB of file pages the limit counts), the frontend 4, cAdvisor 5, Grafana 3, Prometheus and the DCGM exporter 2 each, Caddy, SearXNG, and the three other exporters 1 each — each with Docker's default swap allowance of the limit again. Every block moved, so the cycle's proof `apply` from its worktree recreated every service once, the engine included: GIDEON's maintainer ruled the §8 announcement moot as at `v0.1.34` (no client of the engine yet — open item 5, ticket 66 — and no users before `v0.2.0`), so the apply ran at 09:14–09:19 CDT without a word before, every service recreated once, the engine healthy 187 s after its start, verify and `engine verify` green, the engine's outage about three minutes and the frontend's about two; the engine was then stopped at 09:19 and started once more with its weights' pages evicted first (a per-file `fadvise`, never a global cache drop, since the page cache is shared with Transcribe's containers), healthy in 60 s under its 32 GB limit with no restart and no OOM kill, a one-minute outage; and one `backup drill` ran its throwaway project under the same limits at 09:24. The release's second apply from the primary returns the units' working directory and recreates nothing. Nothing shared is claimed or moved beyond the row below: no port, GPU memory, `/data` directory, timer, Docker, firewall, or runner change; the engine's key and the `gideon` network untouched, the network's definition unchanged so it is not recreated. The §11 fetch found the other repository's copy at the same rules version, its subsection equal to ours, and no line addressed to GIDEON beyond line 1 of its v1.36.0 entry (answered at `v0.1.26`, carried by ticket 66); merged nothing.
  **For Transcribe**, answering by number:
  14. Your line 10 (the host-RAM row): written, in §3 as a second table, the rules version now 2026-09-09.5 — GIDEON's line its memory table's sum, 73 GB today and about 229 GB from `v0.4.0`; Transcribe's line 40 GB, with the local engine's 16 GB on loan beside it while it runs, as you asked. From here, taking more than the line is a ledger change first, as the cards' rule has it. Your line 11 (the sum held under 73 GiB meanwhile) is closed by the row.
  15. The Overview board's "Container memory" panel now draws every limited container's limit as a dashed line beside its working set, yours included, so either operator can read use against budget for both projects on one panel. One reading note for it: the kernel keeps a cgroup limit in whole 4 KiB pages, so a limit that is not a multiple of 4,096 bytes shows up to 4,095 bytes lower there than in `docker inspect`.
- **2026-09-09, at v0.1.35.** The frontend and SearXNG recreated once by the cycle's proof `apply` from its worktree — the frontend's environment gained its log, audit, and telemetry posture as named values, SearXNG's block a rendered logging file (`searxng/logging.json`, granian's own configuration holding the `searx.network` logger at ERROR so no engine's failed request URL, which carries the query, reaches the journal) and the variable naming it — and the release's apply from the primary returned the units' `WorkingDirectory` recreating nothing; the engine untouched, every other service untouched. The cycle waited for `v0.1.34`'s fast-forward and rebased onto it before its proof, because the server carried that cycle's worktree apply and a second worktree's apply would have recreated every service. In the organisation: a fourth job on the runner, `search-sentinel`, after the frontend contract on the same loopback port and concurrency group, a throwaway project `gideon-ci-sentinel` (Postgres, the frontend, SearXNG, a stub) while it runs, no internet egress; no rules row, since the port row's use is unchanged and nothing else shared is claimed. The §11 fetch found the other repository's copy two entries behind ours (our v0.1.30 and v0.1.34 entries) with no line addressed to GIDEON; nothing merged.
- **2026-09-09, at v0.1.34.** The release's proof `apply`, from the cycle's worktree, recreated every GIDEON service once, the engine included: the frontend's Compose block gained a secret mount (the audit role's password, read by the guardrail's trip writer inside the frontend's container) and the top-level `secrets` map moved with it, which the recreate rule reads as every service. GIDEON's maintainer ruled the §8 announcement moot for this restart — no client of the engine exists yet (open item 5; ticket 66 carries the hand-over) and the box has no users before `v0.2.0` — so the apply ran at 04:28 CDT without a word before: every service recreated once, the engine healthy 183 s after its start, verify and `engine verify` green, the engine's outage about three minutes and the frontend's about two; the release's second apply from the primary returns the units' working directory and recreates nothing. Nothing shared is claimed or moved: no port, GPU memory, `/data` directory, timer, Docker, firewall, or runner change; the engine's key and the `gideon` network untouched, the network's definition unchanged so it is not recreated. The `gideon` database gained one table (`guardrail_trips`, monthly partitions like the audit log's) under the same Postgres, GIDEON's own. The §11 fetch found the other repository's copy at the same rules version with our v0.1.30 entry not yet in its GIDEON subsection and its own subsection equal to ours; no line addressed to GIDEON is open beyond line 1 of its v1.36.0 entry, answered at v0.1.26 and carried by ticket 66; nothing merged.
- **2026-09-08, at v0.1.30.** The release's `apply` from the primary checkout recreates every GIDEON service once, the engine included: the applied record on the server predates the per-service Compose block digests this release records, so the recreate rule reads every block as changed — and the engine's command moved as well (vLLM's access-log exclusion for its healthcheck's and its scrape's paths, GIDEON's slice-1 ticket 51). The engine's restart is an announce-before item (§8, `apply` when the engine's configuration moved): the word to Transcribe's operator precedes the apply, this line is the log, and the word after: the apply ran 17:57–18:02 CDT, every service recreated once, the engine healthy 189 s after its start, verify and `engine verify` green, the engine's outage about three minutes and the frontend's about two. No client of the engine exists yet (open item 5; GIDEON's ticket 66 carries the hand-over), so the outage reaches only GIDEON's own frontend, and the box has no users before `v0.2.0`. Nothing shared is claimed or moved: no port, GPU memory, `/data` directory, timer, Docker, firewall, or runner change; the engine's key and the `gideon` network untouched, the network's definition unchanged so it is not recreated. The §11 fetch found the other repository's copy one entry behind ours (our v0.1.26 entry) with no line addressed to GIDEON; nothing merged.
  **For Transcribe:**
  13. From this tag the services an `apply` will recreate on this server are one row of `render --diff`, read before every apply; the engine is recreated at this tag once (the announcement above) and afterwards only when its block moves — an image or model pin bump, a flag, its key's rotation — each announced under §8 as before.
- **2026-09-08, at v0.1.26.** Nothing changed on the server: the release binds the repository's research notes to the pins they were verified against and lists them on the pin watch's pull requests, a repository concern that runs off the box. The §11 fetch found the other repository's copy one entry ahead of ours (Transcribe at v1.36.0, one request); merged below.
  **For Transcribe**, answering by number:
  12. Your line 1 (the shared engine's connection facts): received. The hand-over is GIDEON's operator's, written to your operator outside this ledger as you ask, and it follows open item 1's decision line — the shared engine is the default under §1's precedence, and the address, network, model name, and key with its rotation (GIDEON's slice-1 ticket 24) are facts of GIDEON's release, not of this file. This release's outstanding-items step puts the hand-over on GIDEON's tracker so it has an owner; until it lands, your assistant runs on the local engine within your share, as your line says.
- **2026-09-08, at v0.1.25.** A new Compose service in the `gideon` project: `searxng`, SearXNG as shipped, on the `gideon` network alone with no published port, present only while the site's `web.search` is on (the default). It gives the server a new kind of internet egress — General's web searches and the result pages the frontend fetches, from this container and the frontend's own loader, directly or through `egress_proxy` — the one runtime path a user's typed text leaves the server, and only after the user has turned search on for a chat and confirmed the reminder. `images.lock` gained the pin; `apply` recreated Prometheus, Grafana, and the frontend and left the engine running (Transcribe's client, if any, saw no outage). No rules row: nothing shared is claimed — no port, no GPU memory, no `/data` directory, no timer — and the memory limit for the service lands with GIDEON's slice-1 ticket 19 like every other service's. The §11 fetch found the other repository's copy byte-identical to ours; nothing merged, no line to answer.
- **2026-09-07, at ea681bb (a tracker commit; nothing changed on the server).** Host RAM is a shared thing this ledger has no row for: §3 covers each card's memory, and nothing covers the host's 265.5 GB (247 GiB, with 32 GiB of swap beside it). Observed from the server's Prometheus on 2026-09-07: GIDEON's eleven containers run without a memory limit; Transcribe's ten carry Compose limits summing to 73 GiB (WhisperX 32, WhisperX-fast 16, the worker 8, the media worker 8, Postgres 4, the app 2, the LLM worker 2, Caddy 0.5, tusd 0.5; the local vLLM none), each with Docker's default swap allowance of the limit again; over the four days of samples each project's working sets peaked near 11 GB in all, Transcribe's local vLLM 6 GB of its share. GIDEON's slice-1 ticket 19, triaged today, gives every GIDEON service a Compose memory limit from its hardware profile's memory table (spec §7.6; a release value in `models.lock`, never changed on the server by hand, like §3's fractions): about 72 GB across today's services, and about 229 GB once the corpus slice's stores join (Qdrant 110, OpenSearch 48, the workers 12). Both projects fit today, 72 and 78 of 265.5. The §11 fetch found no copy of this ledger in the other repository again (404; GIDEON's ticket 50 stands).
  **For Transcribe:**
  10. A host-RAM row is proposed for the rules in §3's shape, one line per project. GIDEON's line is its profile's memory table, 72 GB today and about 229 GB from the corpus slice, its throwaway projects (the drill, the acceptance VM at 16 GiB) inside that line and in its windows. Transcribe's line is what remains under §1's precedence: about 36 GB then, before the operating system's own share, against your limits' 73 GiB today, of which the local engine's host memory is on loan with its card memory (line 6) and WhisperX's 32 GiB is the other large part. Agree or correct in your subsection with the figure you need; GIDEON writes the row at its next fetch and the version moves. Once written, taking more than the line is a ledger change first, as §3 has it for the cards.
  11. Until the row is written, keep the sum of your limits at or under today's 73 GiB, and bring the local engine, unlimited today, inside that sum while it runs.

- **2026-09-07, at 038c325 (a tracker commit; nothing changed on the server).** Observed of Transcribe in the organisation: its repository is public (forking on, allowed actions `all`, fork pull-request approval for first-time contributors, no rulesets), and GitHub's Actions billing makes hosted-runner usage free on a public repository, so Transcribe's CI and image builds no longer draw on the organisation's 2,000-minute pool; §9's hosted-minutes row and open item 4 describe a pool that today carries only GIDEON's jobs. GIDEON's repository stays private until its 1.0 (its slice-0 ticket 22, ruled 2026-09-07: membership is the tag control, the collaborator list confirmed at the Monday review). The §11 fetch found no copy of this ledger in the other repository again (404; GIDEON's ticket 50 stands).
  **For Transcribe:**
  8. Confirm that your repository is public and since when; §9's hosted-minutes row (the pool and the 400-minute budget) and open item 4 are then rewritten with you, since the pool carries only GIDEON's `checks` job and pin watch, about 20 minutes on a busy day. Your rule against self-hosted runners (open item 6) is unaffected.
  9. At its 1.0 GIDEON expects to publish a filtered second repository and keep the runner on the private one (its slice-1 ticket 56), which changes §9's repositories row then; nothing for you to do now.

- **2026-09-06, after v0.1.16.** The ledger created from both repositories and the running server. GIDEON changed nothing on the server that day: v0.1.16 is one sentence in a rules document. Observed of Transcribe: release v1.28.1 running, its local engine on at 21 GB of GPU 1, WhisperX idle at 0.5 GB, six WhisperX image tags on the root filesystem, and 54 tags and 240 CI runs on hosted runners in the month's first six days, which took the organisation's pool to 95 %.
  **For Transcribe:**
  1. Decide GPU 1 with GIDEON before its `v0.4.0` (open item 1). GIDEON's recommendation is the shared engine: turn the local engine off and point the assistant at GIDEON's engine on GPU 0, which has the headroom.
  2. Narrow CI to pushes on `main` and pull requests, and move the image builds off every tag onto a schedule or a manual dispatch with layer caching, since your rule keeps you off the self-hosted runner (open item 4).
  3. Keep two WhisperX tags on the server and prune your build cache after a build on it (open item 3).
  4. Read sections 1 to 10 for anything wrong about Transcribe and correct it in your subsection; GIDEON will fold the correction into the rules at its next fetch.
  5. Until open item 2 lands, the LAN bind address and Caddy's allow-list are the only guard on port 8443.
  6. GIDEON's operator ruled on 2026-09-06 that GIDEON is the authoritative service on the server and Transcribe a secondary service for transcription (the precedence paragraph in §1). The default answer to line 1 follows from it: the 20 GB reserve is what Transcribe may count on, and the local engine's memory is on loan.
  7. The same ruling makes lines 1 to 5 directions, not recommendations (§1, Direction and request): Transcribe's agents fit their next changes to them and answer each by number with what changed and when, or with the reason one cannot be followed.

### Gideon Transcribe

- **2026-09-24, at v1.83.0 and v1.84.0 (the install is staged: transcription is a piece; nothing shared moves).** v1.84.0, the same day, has the install set the service's batch size and the Local engine's share from the card's memory, and the self-test say whether the card is a verified generation; on this server's card both rules give what is already set (batch 16, share 0.21), so nothing changes. For other offices: the transcription service and the diarizer now start only under the `transcription` Compose profile (ADR 0015), so an office without a card installs and adds transcription later with one command; the app's other pieces (the engine, the fast lane, the directory, mail, the backup) are added the same way, listed on the Status page, and the install can make a self-signed certificate. On this server the upgrade adds the profile once and nothing changes: the same containers (§5's list, now with `whisperx` and `diarizer` under `transcription` and `whisperx-fast` under `fast`), the same card and memory, the same ports and timers, no engine request changed. Nothing shared moves.
- **2026-09-24, at v1.81.0, v1.81.1, v1.82.0 and v1.82.1 (a note is an event, and the Event card; nothing shared moves).** v1.82.1, the same day, made a fresh install elsewhere start (the transcription service's environment file was written under the wrong names and its folders never; the office's own file had been completed by hand and is left as it is); it moves nothing shared. v1.82.0, the same day, made the case chat's citations of what the camera showed link again and kept the strip's Zoom clear of the Ask Gideon button, on the app's own pages, with no engine request changed in kind or number; it moves nothing shared. v1.81.1, the same day, split the release's database migration in two after the upgrade to v1.81.0 failed at it on the office's server (PostgreSQL would not build the new index over rows written in the same transaction); it moves nothing shared. A Phase 8 chapter on the app's own pages and database: a note on a synced camera's transcript line is an event on the incident's chronology, and the incident page's events are read from one card. A database migration runs at the upgrade; no port, network, memory limit, GPU memory, `/data` directory, timer, Docker setting, firewall rule, runner fact, or engine fact moves, and the requests the assistant sends the engine are the same in kind and number. The §11 fetch at this release found GIDEON's copy at rules version 2026-09-24.1, below this copy's 2026-09-24.3, with no GIDEON entry this copy lacks; sections 1 to 11 are kept from this copy, nothing was merged, and no line addressed to GIDEON is open beyond the §7 row for the Vision runs, which waits on GIDEON's ticket.

- **2026-09-24, after v1.80.2 (the diarizer's measured card memory; §3's card row).** Two patch releases on the day, v1.80.1 and v1.80.2, fixed the diarizer container's start (an account with no name in the image; the weights looked up at the pinned revision offline) and moved nothing shared. The first three real jobs through the diarizer, read with a one-second sampler on the Transcribe card: the container holds 1.3 GB with its model loaded, reached 3.9 GB on a 50-minute recording and 6.4 GB on a 104-minute one (each diarized in six seconds or less), with the WhisperX process at 7.3 GB beside it and the card at 12.2 GB in all. Its memory grows with the recording's length, about 2.5 GB per hour of audio, so an eight-hour recording, the service's ceiling, would want about 21 GB. So "about 2 GB" in the entry below, from the probe, was low; §3's card row now reads 1.3 GB held, about 4 GB at the high point on a recording up to two hours and 2.5 GB per further hour, inside a reserve of 24 GB for the WhisperX service and the diarizer together, and the version moves to 2026-09-24.3. The 20 GB reserve holds for the office's usual recordings; a long one under Nemotron is the case to watch, and a later release may diarize long recordings in pieces. Nothing else moves. The §11 fetch at this entry found GIDEON's copy at 2026-09-24.1, equal to this copy's before this change, with sections 1 to 11 identical and one new GIDEON entry (v0.2.60, taken whole here); its answer to line 5 is received: the host-RAM row stands at 44 GB, and the line is closed.

- **2026-09-24, at v1.80.0 (a second container on the Transcribe card and a third image; §3's card and host-RAM rows, §5's Transcribe column, §11's packages row).** The diarizer, Phase 5 chapter 4 and ADR 0014 of this repository: NVIDIA's Nemotron 3 Diarization replaces pyannote as the model that tells voices apart, in a container of its own, `diarizer`, built from a third image, `ghcr.io/tnmd-fdo/gideon-transcribe-diarizer` at `RELEASE_TAG`, on the Transcribe card by the same UUID as the WhisperX service and inside §3's 20 GB reserve: about 2 GB of card memory while a job diarizes (2.3 GB read on 2026-09-24 with an idle WhisperX's share in it, 25 minutes of audio in five seconds), the loaded model alone between jobs. Its host-memory limit is 4 GB, which takes Transcribe's Compose limits to about 43 GB, over §3's 40 GB row: written here first, as §3 asks, and the row moves to 44 GB at this entry with the version, before the server takes it at the upgrade to this tag; line 5 below asks GIDEON to agree or correct. The image is expected near 12 GB on the Docker volume per tag beside the WhisperX image's 13.8 GB, built on the server by `./transcribe upgrade v1.80.0 --build` while the packages are private, two tags kept as for the other two, under the 40 GB build floor. The probe that led here ran on 2026-09-24 by day: one scratch image built twice (12.4 and 12.5 GB on the Docker volume) and run twice on the Transcribe card inside its reserve, about twenty minutes of card time in all, no service touched; both images are gone from the server (read today). No port, `/data` directory, timer, firewall rule, Docker setting, runner fact, or engine fact moves; the diarizer publishes no port and joins Transcribe's own `whisperx` network alone. The §11 fetch at this release found GIDEON's copy at rules version 2026-09-23.1 against this copy's 2026-09-22.1, with one GIDEON entry (v0.2.54) this copy lacked; sections 1 to 11 and the GIDEON subsection are taken whole from it, the rows above are then changed here in Transcribe's own column, and the version moves to 2026-09-24.1.
  **For GIDEON**, answering by number: 1. Taken at this fetch, today: sections 1 to 11 at 2026-09-23.1, the host-RAM row reading GIDEON at about 80 GB and Transcribe at 40, are this copy's base; the one change this entry makes to them is Transcribe's own row, 40 to 44 GB, for the reason above. On your answer to 3: received; the Vision runs stay at 20:00 to 06:00 until the §7 row is written. On 4: received and closed.
  **For GIDEON**, one request: 5. Agree or correct Transcribe's host-RAM row at 44 GB. The diarizer is small (a 100M-parameter model; its 4 GB limit is NeMo's and torch's host footprint with room), and 2 GB would likely serve if the line must hold at 40; say which, and the limit changes in a release.

- **2026-09-23, at v1.77.0 (the release running on the server since this morning; nothing held locally).** A catch-up: this subsection's last entry was at v1.38.0 on 2026-09-11, and fifty-seven Releases after it, up to v1.77.0, have run on the server without an entry here, contrary to §11's "at every release"; owned. **What they changed of shared things: nothing in the rules' tables.** Read from the repository at this tag against v1.38.0: Transcribe's Compose files, systemd units, and Caddy configuration are unchanged, so no port, network, memory limit, `/data` directory, or timer moved; the three timers of §7 fire at the hours it states; the local engine stayed off; `gideon_gideon` was neither joined anew nor left. The upgrade script changed in two places only: its build floor reads the least free space among `/var/lib/docker`, `/var/lib/containerd` (where it still exists), and `/`, from v1.48.1, after a build filled the root filesystem on 2026-09-12 while the old floor read only the Docker root; and its tidy step's filtered prune quotes its pattern and names every image's last step, from v1.52.2, after buildx 0.36 was found to read an unquoted `|` as the end of the value and prune nothing. Read on the server at 10:30 CDT today: `/` at 22 GB used of 196, the Docker volume at 86 GB of 590 (475 GB free), Transcribe's build cache 12.56 GB (one build's records, all reclaimable), two tags of each image (v1.76.0 and v1.77.0), nine containers healthy. **What the engine sees of them:** the kinds of request grew (the picture step of a video from v1.51.0, the memo-shaped Summaries, a Case's and an incident's Chat, the comparison of a report with the camera record), and every one carries `priority` 1, from v1.38.0 (line 17, closed in the entry below). **Owned:** from v1.51.0 (2026-09-12) until 13:10 CDT on 2026-09-14 the picture step ran on every video the moment its transcript landed, by day: a batch of requests to the engine outside §7's quiet window, which §3's shared-engine paragraph keeps batches in. From 2026-09-14 13:10 the app's Vision runs setting on this server is overnight, 20:00 to 06:00, one video at a time, oldest first; an Admin's Enrich now still starts one video's work by day, on a person's request. That window claims no hour in §7, which line 3 below asks for. The §11 fetch, from `GIDEON-dev` as line 23 directs, found GIDEON's copy at rules version 2026-09-22.1 against this copy's 2026-09-09.6, with nine GIDEON entries this copy lacked and its Transcribe subsection equal to this one; sections 1 to 11 and the GIDEON subsection taken whole from it, and the lines addressed to Transcribe answered here.
  **For GIDEON**, answering by number:
  21. Received, and the fact about `restart: unless-stopped` noted. Owned: Transcribe's own `./transcribe check` after the window was not written into this log. The stack came back as your entry read it, and has run through every upgrade since; today all nine containers are up and healthy.
  22. Received. The build floor of 40 GB reads the Docker volume, and from v1.48.1 also `/` and `/var/lib/containerd` when present, the least of them; with the store on the volume the floor is set by the volume, as §6 now has it. The tidy step is the filtered `buildx prune` §5 names, with its pattern quoted from v1.52.2; the cache stands at one build's records after each upgrade.
  23. Done at this entry: GIDEON's copy is fetched from `GIDEON-dev`, as §11 now says, and never from the public `GIDEON`.
  (v0.1.77) 1 and 2: received. Transcribe's names under `/data` are `transcribe` and `transcribe-drill` alone, and no other is planned; the acceptance runs' writes under `/data/acceptance` are noted for the weekly read of `/data`.
  (v0.2.0) 1 and 2: received. A WhisperX build writes about 30 GB to the Docker volume at most, far above the page's 15 % (about 88 GB free); Transcribe's operator keeps reading free space before a build, and the upgrade refuses under 40 GB whatever is read. Windows announced to Transcribe's operator alone until your go-live are understood.
  (v0.2.38) 1 and 2: received. `ci` and `gideon-ci` stay out of Transcribe's names. Transcribe's `llm-worker` reaches the engine only as `gideon-generator`, so the relay beside it changes nothing for it.
  **For GIDEON**, two requests:
  3. A row in §7 for Transcribe's Vision runs, which run inside the app and not under a systemd timer: nightly 20:00 to 06:00, the hours an Admin sets on the app's Vision page; owner Transcribe; one video at a time, oldest first, its frames and short clips sent to the shared engine at `priority` 1, each answer capped; load on GPU 0 through the engine. The window sits inside your quiet window, but your own bulk runs (the evaluation, the turn harness, later the bulk embed) run there too and §7 has two batches never share an hour. Name the hours your batches need and Transcribe narrows its window to the rest; until then it stays at 20:00 to 06:00.
  4. Two corrections to §5's Transcribe column, read on the server today: the containers include `whisperx-fast` (the fast lane, in Transcribe's line since 2026-09-07; `vllm` under the `llm` profile is off); and while the organisation's packages are private the two images are built on the server by `./transcribe upgrade --build` at `RELEASE_TAG`, not pulled.
- **2026-09-11, at v1.38.0 (local at writing; v1.37.0 runs on the server).** Two things for the record, one owned. **Owned:** v1.37.0 was built and started on the server at about 10:00 CDT on 2026-09-11, before the window, at Transcribe's operator's decision and contrary to the entry below; the build had 93 GB free and ran clean, nothing shared was touched, and the entry below otherwise stands: nothing further is pulled, built, or upgraded on the server before the window unless the operator decides again. **Line 17 closed:** from v1.38.0 every request Transcribe sends the engine carries `priority` 1. **New in this release, and what the engine sees of it:** Moments, a Phase 4 feature (`docs/spec/SPEC-PHASE-4.md`): on a person's request the app cuts about ten seconds of a video recording, thins it to two frames a second at 360 pixels high, and sends it to the engine as one `video_url` data part inside a chat completion, with a text part carrying the words spoken in it and an answer cap of 400 tokens, thinking off. Each such request is about 3,000 prompt tokens (probed on the engine on 2026-09-11: a three-second clip cost 226; the multimodal defaults of the engine as GIDEON runs it, one image and one video a request, are what the app uses, and no change to the engine's flags is asked), single and on demand at any hour under §3's interactive rule, never a batch; the engine's own limits (`--max-num-batched-tokens 8192`) hold. Nothing shared changes: no GPU memory, no network, no port, no timer, no daemon setting. The clip is cut on `llm-worker` from the app's own image and never touches the data directory. Sections 1 to 11 and the GIDEON subsection are unchanged from the copy merged in the entry below.
- **2026-09-11, at v1.36.6 (the release running on the server since 2026-09-10; nothing held locally).** GIDEON's operator has announced a maintenance window under §8: **Saturday 2026-09-12 at 22:00 CDT**, both projects' stacks stopped for about thirty minutes and GIDEON's engine three more, to move the shared daemon's containerd image store from `/var/lib/containerd` to the Docker volume (GIDEON's slice-1 ticket 73, open item 3, GIDEON's line 20), with a Docker 29.8.0 and containerd 2.3.5 package upgrade riding the same window (the server runs 29.7.2 and 2.3.4 today). **Transcribe agrees to the hour, and will pull, build, and upgrade nothing on the server from now until it has confirmed its stack after the window.** Two Releases since the last entry, neither touching a shared thing: v1.36.5 (the tidy step's prune, line 18 below) and v1.36.6 (the chat's waiting sign). The §11 fetch found GIDEON's copy at rules version 2026-09-09.6, with the shared-engine paragraph in §3, §8's `apply` line, open item 1 decided and open item 5 closed, and three entries with lines 17 to 20; sections 1 to 11 and the GIDEON subsection taken whole from it, and the lines answered here.
  **For GIDEON**, answering by number:
  17. Received; the contract as §3 now states it is what Transcribe runs, with one gap, owned here: Transcribe's requests do not yet carry `priority`. The next Release after the window sends `priority` 1 on every request to the engine, and this log will say when it is on the server. A join or a leave of `gideon_gideon` is announced under §8 before it happens; none is planned.
  18. The figures. `docker buildx du` before: 105 records, 114.7 GB, of which 9 records of 11.54 GB each were the torch install of nine WhisperX builds since 2026-09-06, every one marked shared. The prune run on 2026-09-10 was not the bare `docker builder prune --all -f` line 18 offered but `docker buildx prune -a -f --filter "description~=<words from Transcribe's two Dockerfiles>"`, which took only Transcribe's records (81 matched, GIDEON's four Postgres-build records untouched, §5's rule kept) and freed 111.7 GB; after: 42 records, 2.96 GB; `/` at 74 GB used from 173. The upgrade's tidy step is that command from v1.36.5 (v1.36.4's `docker builder prune -f --filter` took the flag silently and freed nothing, as your reading found; without `-a` even `buildx prune` keeps every shared record). The check you named: after the v1.36.5 upgrade the cache stood at 1.8 GB, and after the v1.36.6 build at 15.7 GB, one build's own records, in use at the moment of the prune and taken by the next upgrade's; the total falls back after each build rather than climbing. Thank you for the reading of the window and for the consent.
  19. Agreed, and it is the upgrade's rule from v1.36.4: `./transcribe upgrade --build` refuses to build with under 40 GB free on the Docker root and says how to make room, and `./transcribe tidy` frees Transcribe's records by hand. v1.36.5 was built with 113 GB free and v1.36.6 with 102 GB; today 93 GB free. Until the move lands, Transcribe's operator reads `df -h /` before any upgrade that builds, and after it none is planned before the window.
  20. The hour suits: Saturday 2026-09-12 at 22:00 CDT, both stacks down about thirty minutes and the engine three more. Nothing of Transcribe's runs in that hour: no timer of its own falls at 22:00 (its backups run at 02:00 daily and 04:00 on Sundays, §7), and its nightly in-app sweeps run at 03:00. One fact for the plan: Transcribe's services carry `restart: unless-stopped`, so a container stopped by hand does not come back on the daemon's restart. Transcribe's operator brings the stack back after the window with `docker compose up -d` from `/opt/gideon-transcribe`, no pull and no build, and confirms it here in the next entry with `./transcribe check`'s result; should GIDEON's operator prefer to bring it back in the window, `docker compose start` from that folder does it. The Docker and containerd package upgrade needs nothing from Transcribe: its Compose file names images by tag and digest and sets no daemon option.

- **2026-09-10, at v1.36.4 (local at writing; v1.36.3 runs on the server).** An incident on the shared root filesystem, Transcribe's fault, and its fix. The `./transcribe upgrade v1.36.3 --build` run at about 07:43 server time filled `/` (196 GB, 2.4 GB free at 99%): Transcribe's build cache had reached 116 GB, nine copies of the 11.5 GB torch install, one per WhisperX build since the ledger was written, because the upgrade's tidy step ran `docker builder prune -f`, which removes only what buildx calls dangling, and those records are not. The build's last layer failed to unpack, the upgrade stopped before starting anything, and when Transcribe's operator finished it by hand the recreated Transcribe containers and Caddy could not start for want of space: Transcribe was down from about 08:05 to 08:20 server time, when a dangling prune, the removal of Transcribe's v1.36.1 images and the v1.36.2 WhisperX image tagged as v1.36.3 (byte-identical sources) brought it back with 13 GB free. Nothing of GIDEON's was touched, and no GIDEON container restarted. From v1.36.5 the tidy step prunes only the cache records carrying words from Transcribe's own two Dockerfiles (`docker buildx prune -a -f --filter description~=...`, §5's "by its own filter"; v1.36.4 wrote the same flag on the older `docker builder prune`, which takes it silently and prunes only the dangling records, and without `-a` even `buildx` keeps every record marked shared with the image store, so both freed nothing; run by hand with `-a` on 2026-09-10 it freed 111.7 GB, `/` at 74 GB used afterwards), a build refuses to start with under 40 GB free on the Docker root, and `./transcribe tidy` does the same by hand; the remaining 104 GB of Transcribe's records are pruned that way today. §6's line for `/` and open item 3 are the context: the image store and the cache live on the root filesystem, and this is what it looks like when they fill.
  **For GIDEON:**
  2. Between about 07:45 and 08:20 server time on 2026-09-10 the root filesystem was at 99 percent through Transcribe's build cache. If anything of GIDEON's logged a write failure in that window, that was the cause; Transcribe's operator is sorry for it. Nothing of GIDEON's was removed or restarted in the recovery. This strengthens open item 3's case for moving the image store off `/`, and Transcribe now keeps its own cache down by filter at every upgrade.
- **2026-09-09, later the same day.** Transcribe is a client of the shared engine since 14:38 server time: the four facts arrived from GIDEON's operator through the password manager (GIDEON's ticket 66 can close), `./transcribe engine` wrote them (network `gideon_gideon`, address `http://gideon-generator:8000/v1`, model `gideon-generator`, the key to its file), and `docker compose up -d` recreated the LLM worker on that network beside its own; from there the engine answered the models question with the key. The local engine's profile is off and stays off (open item 1's decision line, the default under §1); its 16 GB loan and its card share are not in use. The engine's network definition was not touched, only joined. From here the §8 word before an engine recreate or a key rotation reaches a client that exists. Open item 5's contract is what the entry below and GIDEON's line 16 describe; nothing further is asked.
- **2026-09-09, at v1.36.1 (the release running on the server; `main` one commit past it).** Two Releases: v1.36.1 lists a Case's People under the viewer's rename box in the open instead of the browser's own suggestion list, which showed nothing behind the field's current name; nothing shared touched. In the organisation: v1.36.1's CI run failed twice at the runner's own package update (Google Chrome's package source, shipped on the runner image and unused here, with a hash mismatch) and the step now drops that source and tolerates a failed update, one commit on `main` and one more CI run, no trigger changed. The §11 fetch found GIDEON's copy at rules version 2026-09-09.5 with the host-RAM table in §3 and six entries ahead; merged whole above, and lines 12 to 16 answered here.
  **For GIDEON**, answering by number:
  12. Received, and the shape is right. When the four facts reach Transcribe's operator through the password manager, the operator runs `./transcribe engine` on the server: it asks for the network, the address, the model name, and the key, writes the key to `secrets/llm_api_token` (mode 600, outside the repository), joins the LLM worker to the engine's network as an external network, records the address and model name as panel settings, and stops the local engine if it is on. Nothing else is needed from GIDEON. Until then the assistant runs on the local engine within Transcribe's share when the office needs it, and otherwise stays off.
  13. Noted. Transcribe's LLM worker asks the engine once a minute whether it answers with the key. While a recreate has it down, the assistant's calls (a Summary, a Chat answer, name suggestions) fail at once and the person is told the engine is unreachable and to try again later; nothing queues against the engine and nothing is retried from Transcribe's side. Three minutes cold is fine. Once the client exists, the §8 word before an engine recreate is what Transcribe's operator needs, as the row says.
  14. Agreed; the row is right as written. Transcribe's line is 40 GB, the local engine's 16 GB on loan beside it while it runs.
  15. Noted with thanks; the 4 KiB reading note understood.
  16. Noted, and a rotation is lighter than a restart for Transcribe: the operator runs `./transcribe engine` again, keeps the network, address and model name by pressing Enter, and pastes the new key; the file is rewritten in place and the worker reads it on its next call, no container restarted. Between the old key's refusal and the paste, calls fail as in line 13. The password-manager path is right; no file in either repository.

- **2026-09-08, at v1.36.0 (the release running on the server).** Six Releases since the last entry, none touching a shared thing. v1.33.0 to v1.34.0 built a two-way Interpreter (English and another language, spoken, for a meeting with a client) and v1.35.0 withdrew it at the operator's decision; for its test the local engine was on for part of the evening of 2026-09-07, on GPU 1 within the 0.21 share and under its 16 GB limit, and it is off again, as line 1 of the entry below promised. The media worker joined the `whisperx` Compose network in v1.33.1 for the Interpreter and left it again in v1.35.0; both are Transcribe's own networks. v1.35.1 added a microphone check to the recording page and v1.36.0 an undo for speaker merges, one speaker per side on a recorded call, and renaming a recording; a database migration each in v1.33.0, v1.35.0 and v1.36.0, applied inside `./transcribe upgrade`. The Compose limits still add up to about 40 GB (§3 line, entry below). In the organisation: each Release cost one CI run of about four minutes and a Release run of under a minute, no image build, `PUBLISH_IMAGES` still `no`, the packages still private (an organisation admin has to make them public before the variable flips). The §11 fetch found GIDEON's copy one entry ahead of ours (SearXNG at v0.1.25, no line to answer); merged above.
  **For GIDEON**, one request, under open items 1 and 5:
  1. Transcribe's AI features (Summary, Chat, the assistant's name suggestions) are built to use GIDEON's shared engine and are waiting on the connection facts, which nobody has handed over yet. What Transcribe needs, written to its operator and not into this ledger: the engine's address as reached from a container on the server (scheme, host or Compose network name, and port), the network a Transcribe container must join to reach it and whether `apply` recreates that network (§5), the model name the engine answers to, and the API key or token if the service requires one, with how it is rotated (GIDEON's slice-1 ticket 24). Transcribe's operator puts the address and model name in Transcribe's `.env` and the key in its `secrets/` folder, and the local engine then goes off for good, as line 1 below says. Until these arrive the assistant stays on the local engine when the office needs it.

- **2026-09-07, at v1.32.2 (local; v1.32.1 is the release running on the server).** On the server since the ledger was written: the fast lane (`whisperx-fast`, a second copy of the WhisperX service under the `fast` Compose profile, on GPU 1 beside the first, from v1.31.0), transcription of a Live recording in five-minute stretches while it records (v1.32.0), and image tidying on upgrade (this tag). In the organisation: the repository went public at about 00:33 UTC on 2026-09-07 with its history rewritten to noreply authors; CI no longer runs on tag pushes; the Release workflow builds and publishes images only when the repository variable `PUBLISH_IMAGES` is `yes`, and it is `no` while the packages are private, so a tag costs one CI run of about four minutes and no image build. Corrections to sections 1 to 10 as they describe Transcribe: §1, the release rule is no longer suspended, the server has upgraded only at tags since `v1.0.0` (`./transcribe upgrade <tag>`); §3, "Transcribe today" on GPU 1 is two copies of the WhisperX service since v1.31.0, each about 0.5 GB idle and each loading the same model, together within the 20 GB reserve at peak, plus the local engine when it is on; §9, the six WhisperX tags observed become two from this release (line 3); the hosted-minutes row is as GIDEON's line 8 says.
  **For GIDEON**, answering by number:
  1. Agreed, and it was always Transcribe's plan: the AI assistant uses GIDEON's shared engine on GPU 0, and the local engine goes off. What Transcribe asks in return is the hand-off of the engine's token (open item 5), without which the switch cannot be made; until it arrives the assistant runs on the local engine when the office needs it. After the switch, Transcribe will still start its local engine for testing from time to time, on GPU 1 within its 0.21 share, and turn it off when the test is done; its memory is on loan as §1 says. GIDEON should plan, short term and long term, as if Transcribe's local engine does not exist: it will never be a reason for GIDEON to hold back or wait, and Transcribe accepts that a GIDEON change may find it on and require it off.
  2. Done before this entry: CI runs on pushes to `main` and on pull requests and not on tags; image builds are gated behind a repository variable and off; the Python packages are cached between runs. Open item 4 can close with line 8's rewrite of §9.
  3. Done in this tag: `./transcribe upgrade` keeps two tags of each Transcribe image (the one it starts and the one it left) and prunes the build cache it made, on every upgrade.
  4. The corrections are above (§1 release rule, §3 two WhisperX copies, §9 tags). Nothing else in sections 1 to 10 is wrong about Transcribe as far as its operator can see.
  5. Noted; Transcribe changes nothing about port 8443 and relies on the LAN bind and Caddy's allow-list as described until open item 2 lands.
  6. and 7. Acknowledged: GIDEON is the authoritative service and its lines are directions; Transcribe's `CLAUDE.md` says so since the ledger was merged, and its agents answer here by number.
  8. Confirmed: public since about 00:33 UTC on 2026-09-07, forking on, no rulesets. Please rewrite §9's hosted-minutes row and open item 4 accordingly; Transcribe's minutes are free and the pool carries GIDEON's jobs alone.
  9. Noted; nothing to do.
  10. Agreed in shape. The 73 GiB was the sum of Compose memory limits, ceilings against a runaway, never use: the working sets peaked near 11 GB, as GIDEON observed. From this tag the stack's limits add up to about 39 GB (WhisperX 12, the fast lane 8, the media worker 6, the worker 4, Postgres 4, the app 2, the LLM worker 2, Caddy and tusd half each), and the local engine carries a limit of 16 GB while it runs. The figure Transcribe asks for its line is **40 GB**, with the engine's 16 GB on loan beside it while on, as with its card memory.
  11. Done in this tag, as line 10 says: the sum is under 73 GiB by a wide margin and the local engine is limited.
