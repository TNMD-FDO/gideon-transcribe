# What the tests need. They run on a hosted runner with no database and no
# media tools, so anything that needs either is exercised behind a boundary
# the tests can replace.
Django==6.1.1
psycopg[binary]==3.3.5
procrastinate==3.9.0
whitenoise==6.12.0
pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.6

# The Word export. lxml is python-docx's own dependency, pinned here so
# that the image builds the same way every time.
lxml==6.1.3
python-docx==1.2.0

# Directory sign-in over LDAPS. python-ldap alone, not
# django-auth-ldap: the reasons are in ADR 0010.
python-ldap==3.4.7

