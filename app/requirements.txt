# What the app image installs, pinned.
#
# Procrastinate runs the background work on the app's own PostgreSQL, which is
# why there is no Redis and no Valkey here and no scheduler container in the
# compose file. The version is the one the specification names.
Django==6.1.1
gunicorn==26.2.0
psycopg[binary]==3.3.5
procrastinate==3.9.0
whitenoise==6.12.0

# The two guides are Markdown in docs/, rendered to HTML when the image is
# built so that what the app serves is the Release's own text (ADR 0012).
# Pure Python, no compiled part.
Markdown==3.10.3

# The AI assistant talks to its engine through the engine's OpenAI-compatible
# API, chat completions only, with this client and nothing else.
openai==3.8.0

# The Word export. lxml is python-docx's own dependency, pinned here so
# that the image builds the same way every time.
lxml==6.1.3
python-docx==1.2.0

# Directory sign-in over LDAPS. python-ldap alone, not
# django-auth-ldap: the reasons are in ADR 0010.
python-ldap==3.4.7

